/* SAVE De la première image

--------- Photomaton -----------
Taille image reduite : IMAGE_SIZE / (stepNumber - 1)^2
Nombre d'image à insert : (stepNumber - 1) * 4
--------------------------------
 */


const DEFAULT_IMAGE_SIZE = 256;

var stepNumber = 1;

// var image = new Image();

// image.src = "img/macron.jpg";

// var imageSize = image.width;

